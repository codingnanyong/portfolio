apiVersion: apps/v1
kind: Deployment
metadata:
  name: realtime-service
  namespace: flet-montrg
  labels:
    app: realtime-service
    version: v1.0.0
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: realtime-service
  template:
    metadata:
      labels:
        app: realtime-service
        version: v1.0.0
    spec:
      containers:
      - name: realtime-service
        image: flet-montrg/realtime-service:latest  
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: APP_NAME
          valueFrom:
            configMapKeyRef:
              name: realtime-service-config
              key: APP_NAME
        - name: APP_VERSION
          valueFrom:
            configMapKeyRef:
              name: realtime-service-config
              key: APP_VERSION
        - name: ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: realtime-service-config
              key: ENVIRONMENT
        - name: HOST
          valueFrom:
            configMapKeyRef:
              name: realtime-service-config
              key: HOST
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: realtime-service-config
              key: PORT
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: realtime-service-config
              key: LOG_LEVEL
        - name: CORS_ORIGINS
          valueFrom:
            configMapKeyRef:
              name: realtime-service-config
              key: CORS_ORIGINS
        - name: DEBUG
          valueFrom:
            configMapKeyRef:
              name: realtime-service-config
              key: DEBUG
        - name: LOCATION_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: realtime-service-config
              key: LOCATION_SERVICE_URL
        - name: THRESHOLDS_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: realtime-service-config
              key: THRESHOLDS_SERVICE_URL
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: realtime-service-secret
              key: DATABASE_URL
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 30
