# Docker 컨테이너 호스트명(postgres)은 Pod DNS에 없음. 같은 네임스페이스에서는 docker-postgres 로 접속.
# selector 없음 → Endpoints 수동 관리
apiVersion: v1
kind: Service
metadata:
  name: docker-postgres
  namespace: flet-montrg
spec:
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
    protocol: TCP
