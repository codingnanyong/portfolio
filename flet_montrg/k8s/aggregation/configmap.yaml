apiVersion: v1
kind: ConfigMap
metadata:
  name: aggregation-service-config
  namespace: flet-montrg  
  labels:
    app: aggregation-service
data:
  APP_NAME: "Flet Montrg Aggregation Service"
  APP_VERSION: "1.0.0"
  ENVIRONMENT: "production"
  HOST: "0.0.0.0"
  PORT: "8000"
  LOG_LEVEL: "INFO"
  CORS_ORIGINS: "[\"*\"]"
  DEBUG: "false"
